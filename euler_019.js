function countingSundays() {
  let totalSundays = 0;
  let years = 0;
  let months = 0;
  let dayOfWeek = 0;
  for (let i = 0; i < 12_000; i++) {
    months++;
    console.log(months);

    if (months === 2) {
      dayOfWeek = 
    }
    if (months === 12) {
      months = 0;
      years++;
    }

    // console.log(years);
  }
}

function helper(daysInMonth, startingDayOfWeek) {
    for (let i = 0; i <= daysInMonth; i++) {
        
    }
}

countingSundays();
